# Backend - Professor Do Campo

Este diretório contém o backend (FastAPI) do Professor Do Campo.

Rápido:
- Rodar com Docker: `docker-compose up --build`
- Endpoint docs: `http://localhost:8000/docs`

Configure `.env` com `DATABASE_URL` (Neon) e `REDIS_URL` (Upstash).
